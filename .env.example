# Environment variables for Arac-Takip
APP_ENV=development
# Default brand when launching without the KNK/NKK picker (CLI scripts)
APP_BRAND=knk
# MySQL connection details for phpMyAdmin hosting
DB_HOST=w01df556.kasserver.com
DB_PORT=3306
DB_USER=d03ce6af
DB_PASSWORD=214151Kka
# Shared schema that already exists on the host. Tables will be created with
# `knk_` / `nkk_` prefixes inside this schema.
DB_SHARED_NAME=d03ce6af
